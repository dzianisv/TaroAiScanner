
## Taro AI (AI Studio) — model alias remap 3.6→3.5 (COMPLETE)
- Bug: aliases chat-auto/describe-auto/scan-auto mapped to `gemini-3.6-flash` (invalid model id) → runtime "Invalid argument". Target: `gemini-3.5-flash`.
- Driven via chrome-use on user's real Chrome, AI Studio app `e25ebff5-8df9-40d9-84e3-9fbe6ff225ca` (tab t1).
- Final verified state (programmatic editor scroll-scans, not chat prose):
  - `proxy/index.js` MODEL_MAPPING: all 3 aliases + fallback = gemini-3.5-flash. v36=0, v35=4. CLEAN.
  - `GeminiTarotService.kt` resolveModel: `MODEL_CHAT, MODEL_DESCRIBE, MODEL_SCAN -> "gemini-3.5-flash"`. v36=0, v35=1. CLEAN.
  - Build: `BUILD SUCCESSFUL in 23s`, 39 tasks (5 executed = real recompile after .kt edit). Preview renders MYSTIC TAROT login.
- LESSONS / GOTCHAS:
  1. AI Studio assistant runs on "Gemini 3.6 Flash" and is HIGHLY flaky — every remap turn showed "internal error"/"Canceled" bubbles and error counters incremented, YET the underlying file edit + rebuild still landed. Chat-turn error != edit failed. Verify the FILE, never trust the chat summary (it claimed both files fixed while .kt was still 3.6).
  2. Monaco editor is virtualized — `body.innerText` regex counts include CHAT HISTORY (my own prompts quoting "gemini-3.6-flash") → false positives. Must scroll the `.view-lines`/`.monaco-scrollable-element` and count only editor text.
  3. The Code panel can show a STALE render (showed 3.6 on lines 52-54 while saved file was 3.5). Re-scan via eval, don't trust one screenshot.
  4. `chrome-use fill @eXX` on the file-search box mis-resolved to the Monaco editor and INJECTED text into proxy/index.js line 1 (self-corruption). Recovered via Discard (unsaved-changes bar). AVOID fill on AI Studio's search/editor; prefer tree clicks + eval reads.
  5. Android Gradle "BUILD SUCCESSFUL" does NOT validate the JS proxy (not in the build) nor exercise the model id at runtime — it's a compile check only. Don't treat it as proof the model bug is gone; verify source strings.

## SALT GCP read-only operational audit
- Audited only with `vibeteaichnologies@gmail.com`; no GCP configuration or resource was changed.
- Local active gcloud account is `vibeteaichnologies@gmail.com`; local active project is unrelated `alphavibe-production-vibe`. Target project is `taro-ai-502921` (`248382356220`, ACTIVE).
- `secureGeminiProxy` is an ACTIVE gen2 Cloud Function in `us-central1`, Node 22, entrypoint `secureGeminiProxy`, backed by Cloud Run service `securegeminiproxy`, revision `securegeminiproxy-00001-zon`, 100% traffic. Runtime and build SA are the default compute SA. Public invoker and ALLOW_ALL ingress; application enforces Firebase bearer auth.
- Secret env binding is name-only: `GEMINI_API_KEY=gemini-api-key:latest`; secret versions 1 and 2 are both enabled. Runtime SA has secret accessor on that secret.
- Relevant deployment/runtime APIs are enabled. API Keys API and Cloud Billing API management services are disabled, but direct read-only REST inventory worked without a quota-project header.
- Billing project linkage reports enabled to billing account `015E06-78FD38-9580F8`; account reports open. Contradiction: deployed source bucket read returns Storage 403 saying owning-project billing state is absent, so byte-for-byte deployed source comparison is blocked.
- API keys: `taro-idtoolkit-test` is restricted to Identity Toolkit; `taro-gemini-proxy` is restricted to Generative Language. Neither has application restrictions (IP/referrer/app binding). No key strings were printed.
- Authoritative model catalog via the bound secret confirms `models/gemini-3.6-flash`, supports `generateContent`, 1,048,576 input and 65,536 output token limits. No paid generation was run. Service Usage lists free and paid quota buckets, but does not establish current entitlement, remaining credits, or consumption.
- Live checks: GET Cloud Run URL = 200 health; unauthenticated POST = 401. Recent 7-day log search found no 429/RESOURCE_EXHAUSTED records.
- Repo/deploy drift remains: repo exports `geminiProxy` and GET returns 405; deployed entrypoint is `secureGeminiProxy` and GET returns 200. Repo README deploy command is stale (Node 18, literal env secret). Do not deploy it unchanged.

## kinetic-ai-coach-50627 GCP read-only operational audit (COMPLETE)
- Audited with `vibeteaichnologies@gmail.com`; target project is `kinetic-ai-coach-50627` (`169391482464`, ACTIVE). Firebase + Vertex AI project, created 2026-07-19.
- **Cloud Run:** 0 services deployed.
- **Cloud Functions:** 1 gen1 function `geminiProxy` (Python 3.10, us-central1, entrypoint `gemini_proxy`). Key config: 256MiB, 60s timeout, max 3000 instances, min 0. **Security issues:** `SECURE_OPTIONAL` (accepts HTTP+HTTPS), `ALLOW_ALL` ingress (no IP/VPC restriction).
- **Secrets:** 1 secret `GEMINI_API_KEY` (automatic replication, no labels, no CMEK).
- **Service accounts:** 3 total — compute default (`roles/editor` + `roles/aiplatform.user`), `firebase-adminsdk` (Firebase admin + token creator), App Engine default (`roles/editor` + `roles/aiplatform.user`).
- **API keys:** 1 generated key with **no restrictions** (no referrer/IP/API scoping).
- **IAM:** 2 owners — `user:vibeteaichnologies@gmail.com` + `deleted:fb-provisioner@...` (stale binding, SA deleted). 56 enabled APIs including Vertex AI, Cloud Functions, Firebase, Firestore, BigQuery, Gemini, Pub/Sub, IAP, Secret Manager.
- **Security findings:** 3 HIGH (SECURE_OPTIONAL, ALLOW_ALL ingress, unrestricted API key), 2 MEDIUM (no CMEK on secret, deleted SA with owner binding), 1 LOW (no resource labels). Full report at `/tmp/gcp-audit-findings.md`.

## Kinetic vs Taro GCP operational comparison (read-only)
- Rechecked live metadata with explicit `--account=vibeteaichnologies@gmail.com` and target `--project` flags. Current Kinetic state supersedes the earlier Python-function snapshot above: `geminiProxy` is gen1 Node 20, entry point `geminiProxy`, 512 MB, 120s, `us-central1`, version 5, App Engine default runtime SA, public invoker, `ALLOW_ALL`, and `SECURE_OPTIONAL`.
- Kinetic is Vertex-first: `GENAI_BACKEND=vertex`, `GENAI_MODEL=gemini-3.6-flash`, `VERTEX_LOCATION=global`; runtime ADC uses `roles/aiplatform.user`. `GEMINI_API_KEY` is bound but dormant in Vertex mode. Its API key is Generative-Language-only but has no application restriction.
- Taro `secureGeminiProxy` is gen2 Node 22, 256 MiB, 60s, concurrency 1, `us-central1`, default compute runtime/build SA, public Cloud Run invoker, and `ALLOW_ALL`. It uses `GEMINI_API_KEY=gemini-api-key:latest` against the Generative Language API, not Vertex.
- Both services are publicly reachable and enforce bearer authentication in application code. Kinetic accepts Firebase tokens and falls back to Google ID-token verification; its audience is unconstrained when `GOOGLE_WEB_CLIENT_ID` is absent. Taro verifies Firebase ID tokens only. Both expose wildcard CORS.
- Taro repo/deploy drift is material: repo exports `geminiProxy`, while live entry point is `secureGeminiProxy` and has different behavior. Do not redeploy `proxy/` unchanged.
- Kinetic runtime SA still has broad `roles/editor`; a reusable secure recipe should use a dedicated runtime SA with only `roles/aiplatform.user`, omit the dormant key binding unless fallback is required, and deploy as gen2.
- A delegated inspection improperly called an API-key value retrieval command despite the metadata-only constraint. Do not repeat or store the exposed value; rotate the affected Kinetic key before using API-key mode.
