# 2026-07-23 — Play release takeover (ses after 072856b6bffe)

## AAB verified (source of truth = decoded AAB proto manifest, NOT gradle text)
- app/build/outputs/bundle/release/app-release.aab: package com.aistudio.mystictarot.qxrptl, versionCode=2, versionName=1.1, 41,395,277 bytes, sha256 bdbd8b7b3e88321dce8ce332a3842f7b5ea86d3e22fc9ee8edf647f444b68472.
- NOTE: working-tree app/build.gradle.kts still says versionCode=1/versionName="1.0" — STALE vs the built artifact. The AAB is genuinely "v2". No rebuild needed.

## Non-browser publish path is DEAD (org policy)
- Enabled androidpublisher.googleapis.com on taro-ai-502921 (this session).
- Created SA play-publisher@taro-ai-502921.iam.gserviceaccount.com (this session).
- SA JSON key creation BLOCKED by org policy constraints/iam.disableServiceAccountKeyCreation (returns 0-byte file). Removed the empty file. => fastlane supply impossible without org-admin policy override (out of scope; AGENTS.md forbids touching GCP policy). Also the Play Console "Setup > API access" SA grant is browser-only regardless.
- No Play service-account JSON in Bitwarden (only upload keystores + Gemini key).

## Remaining work is BROWSER-ONLY, blocked on chrome-use "Allow" (queued to founder)
- Full deterministic runbook written: thoughts/play-release-runbook.md (AAB coords, upload steps, subscription spec, prereq blockers).
- Subscription spec: product id mystic_tarot_premium_monthly, "Mystic Tarot Premium (Monthly)", monthly auto-renew $4.99 USD.
- Prior note claim "vc=1 closed release submitted for review" is UNVERIFIED — must read true Play track state in browser before assuming.

## Play release + subscription COMPLETED (browser, chrome-use, /u/3/ vibeteaichnologies)
- chrome-use un-wedged (founder clicked Allow). Drove Play Console directly.
- TRUE closed-testing state VERIFIED (predecessor "in review" claim was stale/understated): Track "Closed testing - Alpha" ACTIVE, release 2 (1.1), status "Available to selected testers", released Jul 23 4:15 AM, 177 countries. Testers = "Mystic Tarot Testers" email list (1 user). => AAB v2 upload + review already DONE and LIVE; no re-upload needed.
- SUBSCRIPTION CREATED + ACTIVE: product "Mystic Tarot Premium (Monthly)" id mystic_tarot_premium_monthly; base plan "monthly-autorenew" = Monthly auto-renewing, $4.99 USD applied across 177 regions (auto-converted), grace 7d / 53d account hold (auto), Resubscribe=Allow. Base plan status = Active (green check, "Your changes have been saved").
- GOTCHAS: (1) chrome-use has NO isolated profile — it's the user's real Chrome; correct dev account is /u/3/, NOT /u/0/ (u/0 -> signup). (2) Don't guess Play URL slugs (tracks/closed-testing 500s); use nav refs. closed track slug = closed-testing?releaseType=defaultReleases. (3) chrome-use text-clicks drift after SPA nav; use eval-by-textContent or fresh @eN refs. (4) base plan Save fails until prices set for all regions — use Set prices > Select all rows > $4.99 USD > Update, THEN Save, THEN Activate. (5) proxy reconnect can re-trigger Allow dialog mid-run.
- Tester opt-in (email-list closed track, standard pattern): https://play.google.com/apps/testing/com.aistudio.mystictarot.qxrptl

## SA key blocker — root cause (investigated, NOT overridden)
- Constraint constraints/iam.disableServiceAccountKeyCreation is set at ORG level (organizations/191374647072), enforce:true, updated 2026-06-26. This is a DELIBERATE security guardrail (SA JSON keys are long-lived static creds = top leak vector), NOT a Google default (default is unenforced).
- No project-level policy on taro-ai-502921 (NOT_FOUND) => taro inherits the org enforce.
- vibeteaichnologies@gmail.com org roles: resourcemanager.organizationAdmin, billing.admin/creator, iam.workforcePoolAdmin, serviceusage.serviceUsageAdmin, projectCreator/Mover. MISSING roles/orgpolicy.policyAdmin => `orgpolicy.policies.create` DENIED, so cannot set even a project-scoped exception without self-granting policyAdmin first.
- Did NOT self-escalate / did NOT weaken the org policy. Enabled orgpolicy.googleapis.com on taro (read-only need). Play SA play-publisher@taro-ai-502921.iam.gserviceaccount.com exists but has 0 keys.
- Keyless alternative that COMPLIES with the policy: Workload Identity Federation (I hold workforcePoolAdmin) — fastlane/CI gets short-lived tokens, no JSON key. Preferred over disabling the guardrail.

## Closeout (owner decision: org policy stays; no SA keys)
- Wrote docs/release.md: Play API = manual/browser path only (org policy forbids SA JSON keys), WIF = future keyless option, manual runbook, current live state, revenue gaps.
- Final verify: relied on this-session live screenshots (track_manage.png = Alpha Active release 2(1.1) "Available to selected testers"; activate.png = base plan Active $4.99). chrome-use re-wedged at closeout (Allow re-triggered) so no fresh screenshot — prior verification stands.
- REVENUE GAP (subagent-confirmed, read-only): shipped AAB has ZERO billing code. main has no Play Billing dep/BillingClient/paywall/entitlement. Billing exists ONLY on unmerged branch feat/play-billing (PR #4) AND that branch hardcodes wrong product id `premium_monthly` != real `mystic_tarot_premium_monthly` (BillingManager.kt:36) => purchases would fail even after merge. proxy has no receipt verification. So subscription is purchasable in Play but app cannot sell it. Hard blockers: merge PR#4, fix product id, add server-side androidpublisher subscriptions.get entitlement, build+upload billing AAB, attach payout.

## Production-promotion task (browser BLOCKED; prep done non-browser)
- Both browsers down: chrome-use Allow-wedged; vibebrowser relay CONNECTED (relay+extension yes, 27 tools) but EVERY command times out (30-70s) — jammed by the frozen payments tab. Could not read Play live. Did NOT fabricate on-screen gate confirmation.
- #1 GATE (from account type + Google policy, unconfirmed on-screen): account = VIBE TECHNOLOGIES LLC = ORGANIZATION account => EXEMPT from the 20-tester/14-day closed-testing gate (that gate hits Personal accounts created after 2023-11-13). So Production is promotable now. Verify banner in Console before relying.
- #4 VERIFY PAYWALL: IMPOSSIBLE as asked — shipped/testing build has NO billing code. main: no billing dep, no BillingClient/PaywallScreen (grep=0), versionCode=1. Billing only on unmerged feat/play-billing (PR#4), which hardcodes wrong product id `premium_monthly` (BillingManager.kt:36,103,115) != real `mystic_tarot_premium_monthly`. Nothing to verify in-app.
- SEQUENCING FLAG raised to owner: promoting current build to Production ships a non-monetized app. For revenue, merge PR#4 + fix product id + server entitlement + upload billing AAB FIRST, then promote.
- SHIPPED this turn (non-browser): docs/aso-listing.md (title `Mystic Tarot: AI Card Reader`, short desc, category=Lifestyle, tags, data-safety prereqs), docs/release.md updated with Production gate + sequencing. Store assets confirmed present in /tmp/play-assets (icon-512, feature-1024x500, 3 screenshots).

## PR#4 product-ID fix SHIPPED (browser-independent); merge still blocked
- Fixed the real merge blocker: BillingManager.kt PREMIUM_MONTHLY literal `premium_monthly` -> `mystic_tarot_premium_monthly` (+comment). Committed ca87977, pushed to origin/feat/play-billing. Done via isolated worktree .worktrees/billing-fix (now removed), main's uncommitted session changes untouched.
- PR#4 state: OPEN, mergeable=CONFLICTING (DIRTY). Conflicts are ALL in proxy/ (e2e.test.js, package.json) + .gitignore — branch is stale vs main's merged Vertex proxy work. NO conflicts in app/ billing code. Resolving needs main's proxy to win (production-critical) — did NOT auto-merge blindly.
- REMAINING before revenue-safe merge (unchanged): (1) resolve PR#4 proxy conflicts keeping main's Vertex proxy; (2) server-side entitlement (androidpublisher subscriptions.get) — PR#4 is client-trust-only today, insecure to ship as-is; (3) build+upload billing AAB; (4) verify purchase. Did NOT ship trust-the-client billing silently.
- Browsers STILL both down (chrome-use wedged, relay jammed by frozen payments tab). All Play-Console items remain human-blocked.

## Entitlement work — DONE on main (partly mine, mostly parallel agent; NO duplicate PR)
- Resolved PR#4 conflicts (main Vertex proxy won, kept branch billing, product id fixed) commits ca87977+70d64af — PR#4 got MERGED.
- Then implemented full server-side entitlement independently (proxy/lib/verifySubscription.js subscriptionsv2 + POST /verify-subscription route + 10 tests; GeminiTarotService.verifySubscription fail-closed; BillingManager grantIfEntitled server-gated; MainActivity injects verifier). Compiled OK (compileDebugKotlin SUCCESSFUL), pushed to feat/play-billing (d669b04).
- DISCOVERED main ALREADY HAS equivalent shipped via PRs #6-#10 (parallel agent): verifySubscription.js (correct Taro pkg), index.js /verify-subscription route, BillingManager server verify (optimistic-grant-then-revoke design), Billing 9.x, versionCode 2->3, ASO pack, SEO landing, release CI. Main proxy tests = 29/29 PASS (validated in throwaway worktree).
- DECISION: did NOT open a competing PR — main's implementation is complete + shipped + tested. My feat/play-billing branch is redundant (7 behind main). Design diff: main = optimistic grant + server revoke on authoritative-negative; mine = fail-closed (stricter). Both valid; main's already live. Left branch on origin for history.
- Cleaned up worktrees .worktrees/billing-fix + main-verify. Local repo back on main @ b4ade7e.
- NET for revenue: server-side entitlement path EXISTS on main + verified (29/29). Remaining (other agents / owner): Play Console SA androidpublisher grant (in progress by other agent), build+upload billing AAB (versionCode 3), live purchase test. I did NOT upload any AAB (per scope).
