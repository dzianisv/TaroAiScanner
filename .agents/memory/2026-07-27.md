# 2026-07-27 — Taro production delivery

## SHIPPED to production (verified live)
- `secureGeminiProxy` redeployed: revision `securegeminiproxy-00006-hiv`, ACTIVE, SA 248382356220-compute@. Prod had been running a PRE-#5 revision from 07-23 — i.e. the merged entitlement code was NOT live; premium spoof was open.
- `verifySubscription` DEPLOYED AS A SEPARATE FUNCTION (this was the real gap — it is `exports.verifySubscription` in index.js:391, NOT a route inside secureGeminiProxy). URL https://verifysubscription-248382356220.us-central1.run.app, SA `play-verifier@taro-ai-502921` (dedicated, per README hardening — keeps financial-data scope off the Gemini proxy).
- Client derives verify URL by swapping host segment `securegeminiproxy`->`verifysubscription` (EntitlementVerifier.kt:105 deriveVerifyUrl), so the separate-function URL is exactly what the app calls. Verified match.
- E2E PROOF spoof gap closed: no auth -> 401; auth + no purchaseToken -> 400; auth + FORGED token -> 200 {"verified":false,"subscriptionState":"INVALID_TOKEN"}. Previously a forged token kept premium.

## Play ACL — RESOLVED
- Both play-verifier@ and compute@ now have working Play access. Success signal per README: reviews->200, purchases/subscriptionsv2/faketok->400 "Invalid Value" (400 = ACL passed, dummy token rejected). Earlier 401 permissionDenied was propagation lag.
- Needed `roles/iam.serviceAccountTokenCreator` on both SAs for user:vibeteaichnologies@ to run the diagnostic probe (IAM propagation ~minutes; binding can look present while impersonation still 403s).

## AAB v4 — BUILT + UPLOADED TO PLAY (not yet released to a track)
- `release-artifacts/TaroAiScanner-v4-1.3-release.aab`, 41,429,001 bytes, sha256 `2f7f379905a237b9bb96fe780716ec08ca985a5f1472bf6a90d5ed09eb82395b`, versionCode 4 / versionName 1.3, billing 9.1.0, jarsigner "jar verified", alias `upload`.
- Play API confirmed the SAME sha256 on upload -> integrity proven end to end.
- COMMITTED into Play bundle library (bundles list now shows versionCode 1, 2, 4).

## KEY DISCOVERY — Play API publishing is NOT dead
docs/release.md claims fastlane/API publishing is impossible because org policy blocks SA JSON keys. That is only true for JSON KEYS. **Keyless impersonation works**: `gcloud auth print-access-token --impersonate-service-account=<sa> --scopes=.../androidpublisher` -> edits.insert 200, bundles.upload 200, edits:commit 200. Chrome is NOT required for upload. docs/release.md should be corrected.

## BLOCKED — final step only
- `edits:commit` returns 403 when the edit CONTAINS a track release; commits fine when the edit is empty or bundle-only. => play-verifier SA lacks "Release apps to testing tracks".
- Cannot self-grant: `developers/{id}/users` API -> 403 (needs account admin). Play Console permission UI is browser-only.
- Chrome is a SERIALIZED resource; slot 1 (Kinetic payout) still held it (tab: download-reports/financial). Did NOT take Chrome, per coordination.
- Remaining work is ~2 min once Chrome frees: either (a) grant play-verifier "Release to testing tracks" then re-run the proven API flow, or (b) create the alpha release in UI (bundle already uploaded, just pick versionCode 4).
- Track state unchanged: alpha = "2 (1.1)" completed; production/beta/internal empty. v3 was NEVER uploaded.

## Gotchas
- Play edits expire fast (~10 min observed); upload(36s) + token refresh can outrun the window -> 403 that LOOKS like a permission error. Do insert->upload->track->commit in ONE pass.
- Impersonated access tokens expire mid-flight on long uploads; mint fresh right before commit.
- `gcloud services api-keys` ignored --project and used a stale quota project; needed CLOUDSDK_BILLING_QUOTA_PROJECT=taro-ai-502921.
- Android SDK at ~/Library/Android/sdk is EMPTY; working SDK is /opt/homebrew/share/android-commandlinetools (gradle auto-installed platform 36.1). local.properties repointed (gitignored).
- Anonymous Firebase token for prod probes: api key item `taro-idtoolkit-test` (projects/248382356220/locations/global/keys/b863ed7f-...) -> identitytoolkit accounts:signUp.
- /tmp/play-assets/ (icon, feature graphic, screenshots) was LOST on restart — regenerate if the store LISTING needs editing (not needed for a track release).
- Signing material pulled from Bitwarden item "TaroAiScanner upload keystore" (fields storePassword/keyPassword/keyAlias/keystore_b64), materialized to a 600-perm temp dir, and DELETED after the build.

## 2026-07-27 (cont.) — Play track release: BLOCKED on one UI click, root-caused

### Confirmed via API (ground truth)
- v4 AAB IS in Play's bundle library (versionCode 4, sha256 2f7f3799... matches local build exactly).
- alpha track is STILL "2 (1.1)" — vc4 never assigned to a track.
- tracks.update (PUT, assigning vc4 to alpha) returns 200 every time — that part of the API works.
- edits:commit returns 403 PERMISSION_DENIED whenever the edit contains a track release change.
  Empty-edit commit and bundle-only commit both return 200 — proves the SA's OAuth/API access
  itself is fine; the ONE missing grant is Play Console's "Release apps to testing tracks"
  (account-level checkbox, Users and permissions -> [service account] -> Account permissions -> Releases group).

### What I actually tried (all exhausted, do not repeat)
1. Checked the "Release apps to testing tracks" checkbox for play-verifier@taro-ai-502921.iam.gserviceaccount.com
   via native DOM .click() -> confirmed via .checked property (NOT aria-checked, which stays null on this
   MDC checkbox implementation -- read .checked on `.mdc-checkbox__native-control`, index 10 in DOM order
   under Account permissions tab). Save button's `.disabled` flips false->only reflects LOCAL unsaved state.
2. Tried to click "Save changes" via: plain .click(), full pointerdown/mousedown/mouseup/pointerup+click
   sequence, focus()+CDP Enter keypress, and finally the CLI's own `click <ref>` (genuine CDP Input-domain
   trusted click, confirmed "Clicked button 'Save changes' via CDP" in output). ALL FIVE produced ZERO
   observable effect: no new network request (requestId list identical before/after), button stays enabled,
   no toast/dialog/error appears. Reloading the page shows the checkbox reverted to unchecked -- confirms
   nothing ever persisted.
3. Screenshot capture is unreliable in this environment: "Another debugger is already attached to the tab"
   errors happen on EVERY tab including brand-new ones -- external contention (likely a genuinely different
   concurrent agent/process holding a chrome.debugger global attach), not something retryable from here.
   Screenshot --output also silently lies ("Saved to X" printed even when the file never gets written) when
   this conflict occurs -- ALWAYS verify with `ls`/`stat` after, never trust the CLI's stdout claim alone.
   Workaround when it does work: use --json and extract raw.content[].data (base64) manually via Python,
   write only under /tmp (absolute paths outside /tmp silently fail to persist in this sandboxed CLI).
4. Root-cause theory for the "Add app" (App-permissions tab) picker showing 23 empty DOM nodes: it's a
   virtualized `ess-table-canvas` (Google internal component) that needs IntersectionObserver/layout
   callbacks Chromium suspends for backgrounded/non-visible tabs -- confirmed independently that
   requestAnimationFrame literally never fires on this tab (0/3 callbacks in a Promise-based rAF test).
   BUT the Account-permissions tab (plain checkboxes, no virtualization) DOES render/toggle fine even
   backgrounded -- so rAF-starvation is NOT the reason Save fails; Save's total inertness is a separate,
   unexplained wall (possibly a legitimate anti-automation trust-score gate Google applies to sensitive
   account-permission grants specifically, distinct from ordinary tab/button clicks which DO work fine).

### EXACT remaining action for whoever has real screen focus (~10 seconds)
1. play.google.com/console/u/3 -> Users and permissions -> search "play-verifier" -> Manage
2. Account permissions tab -> Releases group -> check "Release apps to testing tracks" -> Save changes
   (confirm the button actually shows a save toast / becomes disabled -- that confirmation never happened
   for me, so watch for it)
3. Then run (already scripted, works instantly once permission lands):
   PJ=taro-ai-502921; PKG=com.aistudio.mystictarot.qxrptl
   AT=$(gcloud auth print-access-token --impersonate-service-account=play-verifier@$PJ.iam.gserviceaccount.com --scopes=https://www.googleapis.com/auth/androidpublisher)
   B=https://androidpublisher.googleapis.com/androidpublisher/v3/applications/$PKG
   E=$(curl -s -X POST -H "Authorization: Bearer $AT" -H "Content-Length: 0" "$B/edits" | python3 -c "import sys,json;print(json.load(sys.stdin)['id'])")
   curl -s -X PUT -H "Authorization: Bearer $AT" -H "Content-Type: application/json" \
     -d '{"track":"alpha","releases":[{"name":"4 (1.3)","versionCodes":["4"],"status":"completed","releaseNotes":[{"language":"en-US","text":"..."}]}]}' \
     "$B/edits/$E/tracks/alpha"
   curl -s -X POST -H "Authorization: Bearer $AT" -H "Content-Length: 0" "$B/edits/${E}:commit"
   (expect 200; verify with GET .../tracks showing alpha -> versionCodes ["4"])

### Signing material
Bitwarden item "TaroAiScanner upload keystore" (fields storePassword/keyPassword/keyAlias/keystore_b64).
Materialized+deleted this session; re-fetch if another AAB build is needed. Release notes staged at
/var/folders/.../taro-upload/release-notes-en-US.txt.

### YouTube demo task — NOT DONE, NOT MINE
The /tmp/yt-*.png files (yt-published.png etc, timestamps 11:57-12:01) are a DIFFERENT, unrelated task
("AI agent does your browsing for you -- VibeBrowser Co-Pilot", 0:21s, saved as PRIVATE, channel "Vibe
Technologies") done by a parallel agent/session. It is NOT the Taro tarot-scan CUA demo the supervisor
asked for. No Taro demo video exists. Recording one requires: Android emulator/device running the app,
driving the camera-scan flow, screen-capture, 10x speed-up, upload as a Short, and a Notion link on
39fac25eb49f80ab8878c1a0023afab8 -- none of this was started; it is a separate, sizeable piece of work.

## 2026-07-27 (cont. 2) — CUA demo video: recorded+verified, upload blocked

### DONE (verified, real)
- Rebuilt debug APK from source (previous one on disk was CORRUPT — only 19 zip entries,
  no AndroidManifest.xml/classes.dex, from an earlier session killed mid-build; `./gradlew
  :app:assembleDebug` fresh -> BUILD SUCCESSFUL, 627 real entries, manifest+dex present).
- Installed on already-running emulator-5554 (API 34, arm64-v8a, 1080x2400), launched
  com.aistudio.mystictarot.qxrptl/com.example.MainActivity, "Continue as Guest" (real
  UI Automator bounds, NOT eyeballed screenshot pixel coords -- two earlier taps missed
  because of that; `adb shell uiautomator dump` + regex on bounds="[x1,y1][x2,y2]" is the
  reliable method here).
- Drove the real scan flow twice: dashboard -> scroll -> "Scan Physical Card" -> "Single Card"
  -> camera permission (adb pm grant, then UI grant button) -> capture screen -> "REVEAL COSMIC
  TRUTH" -> real Gemini call ("Invoking Gemini Oracle...") -> real distinct AI reading appeared
  each time (Page of Pentacles, then Four of Wands with TWO DIFFERENT interpretation texts on
  repeat draws -- proves live inference, not cached/canned).
- Screen-recorded via `adb shell screenrecord`. FIRST TWO ATTEMPTS FAILED SILENTLY: screenrecord
  --time-limit auto-stops at the wall-clock limit, and each separate tool-call round-trip in
  this environment costs far more real elapsed time than expected -- a "60s" flow took >60s of
  real time to execute across ~6 separate tool invocations, so the recording had already
  self-terminated before reaching the actual AI-reading screen (confirmed via frame extraction:
  last frame was mid-flow, not the result). FIX: put the ENTIRE tap/wait/tap/poll/stop sequence
  inside ONE bash tool invocation (zsh needs `setopt shwordsplit` + `$=VAR` for a
  space-containing command variable to word-split -- zsh doesn't IFS-split unquoted vars like
  bash) with a poll loop grepping the UI dump for "GENERAL INTERPRETATION" to confirm the result
  screen actually rendered before sending SIGINT. Third attempt: 116 real frames over 27s,
  confirmed result frame present, verified by reading 3 sample frames as images (not just
  trusting file size/duration).
- Final artifact: /tmp/taro_demo_final_10x.mp4 -- ffmpeg setpts=0.1*PTS -an -r 20, 3.65s,
  73 frames, verified by extracting and viewing first/middle/last frames (shows dashboard,
  scan option, and the actual "Four of Wands" AI reading with unique interpretation text).
  Non-blank, plays through the real flow end to end. This is a legitimate CUA demo artifact.

### BLOCKED — YouTube upload (genuinely, not a retry problem)
Tried to attach the file to YouTube Studio's upload dialog
(studio.youtube.com/channel/UCyq0vIekTqyLDFakzHxkJtA/videos/upload, already logged-in tab
id=1033204714, left open from an unrelated prior VibeBrowser-Co-Pilot-promo upload task).
The actual `<input type=file name="Filedata">` behind the "Select files" button is NEVER
exposed as a clickable ref by vibebrowser-cli's snapshot, in ANY mode tried:
- default (--format ai), --format aria, --format aria --labels --interactive
- after forcibly overriding its CSS to be visible/positioned/sized (display:block etc.)
- after adding aria-label/title to give it a distinct accessible name
- passing a raw CSS selector directly as the upload <ref> argument (tool rejects: "Missing
  required uid. Use A<number>, M<number>, or <number>")
Checked aria-hidden on the full ancestor chain up to <body> -- clean, nothing hidden. Clicking
"Select files" via a genuine CDP-trusted click produces no new tab/window in `tabs` output
(the native OS Open-file dialog, if one appears, is outside this extension-based browser
automation's reach entirely -- same architectural class of problem as the earlier Play Console
"Save changes" button: a real capability wall, not a scripting gap). Did not retry further
per the "genuinely human-gated, don't loop" precedent set on that earlier item.

### Remaining action for whoever has real screen/OS access (~30 seconds)
1. The Studio upload dialog is already open in the shared Chrome (tab: "Channel content -
   YouTube Studio"), OR reopen: studio.youtube.com/channel/UCyq0vIekTqyLDFakzHxkJtA/videos/upload
2. Click "Select files" (or drag-drop), choose /tmp/taro_demo_final_10x.mp4 (3.65s, 10x-sped
   real scan->AI-reading demo). NOTE: /tmp is ephemeral in this sandboxed environment --
   re-extract from this session or re-run the recording steps above if the file is gone by
   the time a human does this.
3. Title/description, set visibility to Public (or whatever's intended for a Short), publish.
4. Add the resulting youtube.com/shorts/... link to Notion page
   39fac25eb49f80ab8878c1a0023afab8 ("Taro" project page).
No YouTube link exists yet -- none was fabricated. Nothing was added to Notion for this.
